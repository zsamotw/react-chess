import React from 'react'
import Dialog from '@material-ui/core/Dialog'

export default function GameDialog(props: {open: boolean, children: any}) {
  return (
    <Dialog {...props}>
    </Dialog>
  )
}
